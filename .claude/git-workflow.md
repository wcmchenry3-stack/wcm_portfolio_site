# Git Workflow

See [~/.claude/standards/git.md](~/.claude/standards/git.md) for the full workflow.

## Portfolio Specific
- Branch naming includes `a11y/<name>` for accessibility work
- PRs target `dev`; `dev` → `main` triggers Render auto-deploy
- Verify at https://billmchenry.org after deploy
